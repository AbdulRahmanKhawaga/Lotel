<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64">
  <!-- Rounded orange square background -->
  <rect x="0" y="0" width="64" height="64" rx="10" ry="10" fill="#F1692A" />

  <!-- Black H shape built from two vertical bars and a center connector with triangular cutouts -->
  <!-- Left vertical bar -->
  <rect x="13" y="12" width="10" height="40" fill="#000" rx="1" />
  <!-- Right vertical bar -->
  <rect x="41" y="12" width="10" height="40" fill="#000" rx="1" />

  <!-- Center connector but with triangular cutouts to match the provided style -->
  <g fill="#000">
    <!-- Top connector triangle (pointing down) -->
    <path d="M32 22 L38 32 L26 32 Z" />
    <!-- Bottom connector triangle (pointing up) -->
    <path d="M32 42 L38 32 L26 32 Z" />
  </g>
</svg>